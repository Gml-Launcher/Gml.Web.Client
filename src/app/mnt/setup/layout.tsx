import { ReactNode } from 'react';

export const metadata = {
  title: 'Gml Настройка',
};

export default function MntSetupLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

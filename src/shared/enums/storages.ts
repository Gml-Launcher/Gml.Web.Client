export enum StorageType {
  STORAGE_TYPE_LOCALSTORAGE = 0,
  STORAGE_TYPE_S3 = 1,
}

export enum StorageTypeOption {
  STORAGE_TYPE_LOCALSTORAGE = 'Локальное хранилище', // Локально
  STORAGE_TYPE_S3 = 'Объектное хранилище S3', // Хранилище S3
}
